#pragma once

#include <array>
#include <map>
#include <string>
#include <vector>

using namespace std;

using ints2 = array<int, 2>;
using ints3 = array<int, 3>;
using intvec = vector<int>;
using strvec = vector<string>;
